---
permalink: "{{ page.filePathStem }}.vue"
---
<script setup lang="ts">
/* DO NOT EDIT THIS FILE IN /webapp - BUILT USING ELEVENTY IN /docs */
useSeoMeta({
  titleTemplate: '',
  ogTitle: '{{ title }}',
  ogDescription: '{{ description }}',
});

definePageMeta({
  layout: 'blog-post',
});
</script>
<template>
  <div class="min-h-screen bg-gradient-to-br from-slate-50 via-white to-slate-100 dark:from-slate-900 dark:via-slate-800 dark:to-slate-900">
    <!-- Article Header -->
    <div class="relative overflow-hidden">
      <div class="absolute inset-0 bg-grid-pattern opacity-5"></div>
      <div class="relative max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-16 sm:py-20">
        
        <!-- Back to Blog Link -->
        <div class="mb-8">
          <a href="/blog" class="inline-flex items-center text-blue-600 dark:text-blue-400 hover:text-blue-700 dark:hover:text-blue-300 transition-colors duration-200 group">
            <svg class="w-4 h-4 mr-2 transform group-hover:-translate-x-1 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
            </svg>
            <span class="text-sm font-medium">Back to Blog</span>
          </a>
        </div>

        <!-- Article Meta -->
        <div class="mb-6">
          <div class="flex flex-wrap items-center gap-4 text-sm text-slate-600 dark:text-slate-300">
            <div class="inline-flex items-center px-3 py-1 rounded-full bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-300">
              <svg class="w-3 h-3 mr-1.5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd"/>
              </svg>
              {{ date | date("%B %d, %Y") }}
            </div>
            {%- if badge -%}
            <div class="inline-flex items-center px-3 py-1 rounded-full bg-purple-100 text-purple-800 dark:bg-purple-900/30 dark:text-purple-300">
              <svg class="w-3 h-3 mr-1.5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M17.707 9.293a1 1 0 010 1.414l-7 7a1 1 0 01-1.414 0l-7-7A.997.997 0 012 10V5a3 3 0 013-3h5c.256 0 .512.098.707.293l7 7zM5 6a1 1 0 100-2 1 1 0 000 2z" clip-rule="evenodd"/>
              </svg>
              {{ badge }}
            </div>
            {%- endif -%}
          </div>
        </div>

        <!-- Article Title -->
        <h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold bg-gradient-to-r from-blue-600 via-purple-600 to-blue-800 dark:from-blue-400 dark:via-purple-400 dark:to-blue-300 bg-clip-text text-transparent mb-6 leading-tight">
          {{ title }}
        </h1>

        <!-- Article Description -->
        {%- if description -%}
        <p class="text-xl text-slate-600 dark:text-slate-300 leading-relaxed mb-8 max-w-3xl">
          {{ description }}
        </p>
        {%- endif -%}

        <!-- Authors Section -->
        {%- if authors -%}
        <div class="border-t border-slate-200 dark:border-slate-700 pt-6">
          <div class="flex flex-wrap gap-4">
            {%- for author in authors -%}
            <div class="flex items-center space-x-3">
              {%- if author.avatar -%}
              <img src="{{ author.avatar }}" alt="{{ author.name }}" class="w-10 h-10 rounded-full ring-2 ring-blue-100 dark:ring-blue-900/50">
              {%- else -%}
              <div class="w-10 h-10 rounded-full bg-gradient-to-br from-blue-500 to-purple-500 flex items-center justify-center text-white font-semibold text-sm">
                {{ author.name | slice(0, 1) }}
              </div>
              {%- endif -%}
              <div>
                {%- if author.url -%}
                <a href="{{ author.url }}" class="font-medium text-slate-900 dark:text-white hover:text-blue-600 dark:hover:text-blue-400 transition-colors duration-200">{{ author.name }}</a>
                {%- else -%}
                <span class="font-medium text-slate-900 dark:text-white">{{ author.name }}</span>
                {%- endif -%}
                {%- if author.description -%}
                <p class="text-sm text-slate-600 dark:text-slate-400">{{ author.description }}</p>
                {%- endif -%}
              </div>
            </div>
            {%- endfor -%}
          </div>
        </div>
        {%- endif -%}

        <!-- Decorative Line -->
        <div class="mt-8 flex justify-center">
          <div class="h-1 w-24 bg-gradient-to-r from-blue-500 to-purple-500 rounded-full"></div>
        </div>
      </div>
    </div>

    <!-- Article Content -->
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 pb-2">
      <div class="bg-white dark:bg-slate-800 rounded-2xl shadow-sm border border-slate-200 dark:border-slate-700 overflow-hidden">
        <article class="prose prose-lg dark:prose-invert max-w-none p-2 sm:p-8">
          {{ content | safe }}
        </article>
      </div>

      <!-- Article Footer -->
      <div class="mt-12 pt-8 border-t border-slate-200 dark:border-slate-700">
        <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-6">
          <!-- Social Share -->
          <div class="flex items-center space-x-4">
            <span class="text-sm font-medium text-slate-700 dark:text-slate-300">&nbsp;</span>
            <div class="flex space-x-2">
              
            </div>
          </div>

          <!-- Back to Blog -->
          <a href="/blog" class="inline-flex items-center px-6 py-3 rounded-lg bg-gradient-to-r from-blue-600 to-purple-600 text-white font-medium hover:from-blue-700 hover:to-purple-700 transition-all duration-200 shadow-sm hover:shadow-md group">
            <svg class="w-4 h-4 mr-2 transform group-hover:-translate-x-1 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
            </svg>
            <span>More Articles</span>
          </a>
        </div>
      </div>
    </div>
  </div>
</template>

<style scoped>
.bg-grid-pattern {
  background-image: 
    linear-gradient(90deg, rgba(0,0,0,0.1) 1px, transparent 1px),
    linear-gradient(180deg, rgba(0,0,0,0.1) 1px, transparent 1px);
  background-size: 20px 20px;
}

/* Enhanced prose styling */
.prose {
  color: #374151;
}

.dark .prose {
  color: #d1d5db;
}

.prose h1, .prose h2, .prose h3, .prose h4, .prose h5, .prose h6 {
  font-weight: 700;
  background: linear-gradient(to right, #1e293b, #475569);
  background-clip: text;
  -webkit-background-clip: text;
  color: transparent;
}

.dark .prose h1, .dark .prose h2, .dark .prose h3, .dark .prose h4, .dark .prose h5, .dark .prose h6 {
  background: linear-gradient(to right, #e2e8f0, #94a3b8);
  background-clip: text;
  -webkit-background-clip: text;
  color: transparent;
}

.prose h2 {
  font-size: 1.5rem;
  margin-top: 2rem;
  margin-bottom: 1rem;
}

.prose h3 {
  font-size: 1.25rem;
  margin-top: 1.5rem;
  margin-bottom: 0.75rem;
}

.prose p {
  line-height: 1.625;
  margin-bottom: 1rem;
}

.prose code {
  padding: 0.25rem 0.5rem;
  border-radius: 0.25rem;
  background-color: #f1f5f9;
  color: #1e293b;
  font-size: 0.875rem;
}

.dark .prose code {
  background-color: #374151;
  color: #e2e8f0;
}

.prose pre {
  border-radius: 0.5rem;
  border: 1px solid #e2e8f0;
}

.dark .prose pre {
  border-color: #374151;
}

.prose blockquote {
  border-left: 4px solid #3b82f6;
  padding-left: 1rem;
  font-style: italic;
  color: #475569;
  background-color: #eff6ff;
  padding-top: 0.5rem;
  padding-bottom: 0.5rem;
  border-radius: 0 0.25rem 0.25rem 0;
}

.dark .prose blockquote {
  color: #94a3b8;
  background-color: rgba(30, 58, 138, 0.2);
}

.prose a {
  color: #2563eb;
  transition: color 0.2s ease;
}

.prose a:hover {
  color: #1d4ed8;
}

.dark .prose a {
  color: #60a5fa;
}

.dark .prose a:hover {
  color: #93c5fd;
}

.prose ul, .prose ol {
  margin-top: 0.5rem;
  margin-bottom: 0.5rem;
}

.prose li {
  line-height: 1.625;
  margin-bottom: 0.5rem;
}

@media (prefers-reduced-motion: reduce) {
  .group-hover\:-translate-x-1:hover {
    transform: none;
  }
}
</style>
